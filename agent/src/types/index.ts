/*
 * types — tool schemas & message types (NOT implemented in M1).
 * -----------------------------------------------------------------------------
 * Later milestones define the read tools (read_page, read_selection, list_tabs)
 * and the single consent-gated reversible write tool
 * (fill_text_into_focused_field), plus the message envelopes exchanged between
 * the side panel UI, the service worker, and the content script.
 *
 * In M1 there are no tools and no cross-context message protocol yet.
 * -----------------------------------------------------------------------------
 */

export {};
